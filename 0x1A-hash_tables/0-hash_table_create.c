#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "hash_tables.h"

/**
 * main - create hash table
 *
 * Return: NULL
 */
hash_table_t *hash_table_create(unsigned long int size);
{
    unsigned long i = 0;
    for (int j = 0; str[j]; j++)
        i += str[j];
    hash_table_t *ht;

    return (NULL);
}
